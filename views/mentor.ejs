<html>
  <head>
  <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
  </head>
  <body>
    <h1>Questions Asked</h1>
    <table style="width:100%">
      <tr>
        <th>user_id</th>
        <th>Question</th>
        <th>College Major</th>
      </tr>
      <% for (var i = 0; i < length; i++){ %>
        <tr>
          <td> <%= recordSet[i].user_id %> </td>
          <td> <%= recordSet[i].question %> </td>
          <td> <%= recordSet[i].college_major %> </td>
          <td> <button onclick="approve()"> 승인 </button> <button onclick="decline()"> 거부 </button> </td>
        </tr>
      <% } %>
  </table>

  <script>
  var mysql = require('mysql');
  var connection = mysql.createConnection(process.env.DATABASE_URL);
  function approve(){
    connection.query('INSERT INTO Mentor_Questions (user_id) VALUES ("hello")');
  };
  function decline(){

  }
  </script>
  </body>
</html>
